export declare class BubbleService extends android.app.IntentService {
    static constructorCalled: boolean;
    private floatingViewManager;
    onHandleIntent(intent: android.content.Intent): number;
}
